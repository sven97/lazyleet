// Package runner is the local judge: per-language drivers that wrap the user's
// solution, parse test-case input, invoke it in a sandboxed subprocess, and
// compare output. Python first (Phase 5).
package runner
